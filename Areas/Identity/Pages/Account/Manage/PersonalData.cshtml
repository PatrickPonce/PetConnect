@page
@model PersonalDataModel
@{
    Layout = "_Layout.cshtml";
    ViewData["Title"] = "Datos personales";
    ViewData["ActivePage"] = ManageNavPages.PersonalData;
}

<h3 class="mb-3">@ViewData["Title"]</h3>

<div class="row">
    <div class="col-md-12">
        <p>Tu cuenta contiene datos personales que nos has proporcionado. Esta página te permite descargar o eliminar esos datos.</p>
        <p>
            <strong>Eliminar estos datos eliminará permanentemente tu cuenta y no podrá ser recuperada.</strong>
        </p>
        <form id="download-data" asp-page="DownloadPersonalData" method="post">
            <button class="btn btn-primary" type="submit">Descargar datos</button>
        </form>
        <p class="mt-3">
            <a id="delete" asp-page="DeletePersonalData" class="btn btn-danger">Eliminar cuenta</a>
        </p>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <style>
        /* Estilo especial para el botón de eliminar en esta página.
           Lo ponemos aquí para que no afecte a otros botones "danger" del sitio.
        */
        .manage-account-container .btn-danger {
            background-color: #dc3545; /* Rojo de Bootstrap */
            border: none;
            color: white !important;
            font-weight: bold;
            padding: 12px 35px;
            border-radius: 30px;
            font-size: 1rem;
            text-decoration: none;
            display: inline-block;
            transition: all 0.3s ease;
        }
        .manage-account-container .btn-danger:hover {
            background-color: #c82333; /* Rojo más oscuro */
            box-shadow: 0 4px 15px rgba(220, 53, 69, 0.4);
            transform: translateY(-2px);
        }
    </style>
}